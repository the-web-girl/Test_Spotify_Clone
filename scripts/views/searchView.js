// searchView.js - Vue Rechercher

export function renderSearchView(token) {
    const app = document.getElementById('app');
    app.innerHTML = '<h1>Rechercher</h1><p>Fonctionnalité à implémenter (exemple SPA).</p>';
}
