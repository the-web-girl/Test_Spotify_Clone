// libraryView.js - Vue Bibliothèque

export function renderLibraryView(token) {
    const app = document.getElementById('app');
    app.innerHTML = '<h1>Ma Bibliothèque</h1><p>Fonctionnalité à implémenter (exemple SPA).</p>';
}
